{
  "name": "resumebuilderpdfservice",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "This service is responsible for generating PDFs from HTML/CSS using Puppeteer.  It is designed to run in a Docker container to ensure consistent font rendering (specifically the \"Inter\" font).",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "puppeteer": "^24.37.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
